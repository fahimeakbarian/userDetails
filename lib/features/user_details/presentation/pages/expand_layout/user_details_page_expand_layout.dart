import 'package:flutter/material.dart';

class UserDetailsPageExpandLayout extends StatelessWidget {
  const UserDetailsPageExpandLayout({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('custom ui for expanded ui')
    );
  }
}
